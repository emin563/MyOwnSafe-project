diff --git a/node_modules/expo-web-browser/build/WebBrowser.js b/node_modules/expo-web-browser/build/WebBrowser.js
index 0807df7..644036b 100644
--- a/node_modules/expo-web-browser/build/WebBrowser.js
+++ b/node_modules/expo-web-browser/build/WebBrowser.js
@@ -1,5 +1,5 @@
 import { UnavailabilityError } from 'expo-modules-core';
-import { AppState, Linking, Platform, processColor, } from 'react-native';
+import { AppState, InteractionManager, Linking, Platform, processColor, } from 'react-native';
 import ExponentWebBrowser from './ExpoWebBrowser';
 import { WebBrowserResultType, WebBrowserPresentationStyle, } from './WebBrowser.types';
 export { WebBrowserResultType, WebBrowserPresentationStyle, };
@@ -282,7 +282,14 @@ function _onAppStateChangeAndroid(state) {
         return;
     }
     if (state === 'active' && _onWebBrowserCloseAndroid) {
-        _onWebBrowserCloseAndroid();
+        const pendingClose = _onWebBrowserCloseAndroid;
+        queueMicrotask(() => {
+            requestAnimationFrame(() => {
+                if (_onWebBrowserCloseAndroid === pendingClose) {
+                    pendingClose();
+                }
+            });
+        });
     }
 }
 async function _openBrowserAndWaitAndroidAsync(startUrl, browserParams = {}) {
@@ -308,6 +315,17 @@ async function _openBrowserAndWaitAndroidAsync(startUrl, browserParams = {}) {
     _onWebBrowserCloseAndroid = null;
     return result;
 }
+function _afterAuthRedirectCatchup() {
+    return new Promise((resolve) => {
+        requestAnimationFrame(() => {
+            requestAnimationFrame(() => {
+                requestAnimationFrame(() => {
+                    InteractionManager.runAfterInteractions(() => resolve(undefined));
+                });
+            });
+        });
+    });
+}
 async function _openAuthSessionPolyfillAsync(startUrl, returnUrl, browserParams = {}) {
     if (_redirectSubscription) {
         throw new Error(`The WebBrowser's auth session is in an invalid state with a redirect handler set when it should not be`);
@@ -317,10 +335,16 @@ async function _openAuthSessionPolyfillAsync(startUrl, returnUrl, browserParams
     }
     try {
         if (Platform.OS === 'android') {
-            return await Promise.race([
-                _openBrowserAndWaitAndroidAsync(startUrl, browserParams),
-                _waitForRedirectAsync(returnUrl),
-            ]);
+            const redirectPromise = _waitForRedirectAsync(returnUrl);
+            const dismissPromise = _openBrowserAndWaitAndroidAsync(startUrl, browserParams);
+            let result = await Promise.race([dismissPromise, redirectPromise]);
+            if (result.type === WebBrowserResultType.DISMISS || result.type === WebBrowserResultType.CANCEL) {
+                const late = await Promise.race([redirectPromise, _afterAuthRedirectCatchup()]);
+                if (late && late.type === 'success' && late.url) {
+                    result = late;
+                }
+            }
+            return result;
         }
         else {
             return await Promise.race([
@@ -349,7 +373,13 @@ function _waitForRedirectAsync(returnUrl) {
     // Note that this Promise never resolves when `returnUrl` is nullish
     return new Promise((resolve) => {
         const redirectHandler = (event) => {
-            if (returnUrl && event.url.startsWith(returnUrl)) {
+            if (!returnUrl || !event.url) {
+                return;
+            }
+            const incoming = event.url;
+            const matches = incoming.startsWith(returnUrl) ||
+                incoming.replace(/^([a-z][a-z0-9+.-]*:)\/{2,}/i, (_, scheme) => `${scheme}/`).startsWith(returnUrl.replace(/^([a-z][a-z0-9+.-]*:)\/{2,}/i, (_, scheme) => `${scheme}/`));
+            if (matches) {
                 resolve({ url: event.url, type: 'success' });
             }
         };
diff --git a/node_modules/expo-web-browser/src/WebBrowser.ts b/node_modules/expo-web-browser/src/WebBrowser.ts
index 923c8d5..66c035d 100644
--- a/node_modules/expo-web-browser/src/WebBrowser.ts
+++ b/node_modules/expo-web-browser/src/WebBrowser.ts
@@ -2,6 +2,7 @@ import { UnavailabilityError } from 'expo-modules-core';
 import {
   AppState,
   AppStateStatus,
+  InteractionManager,
   Linking,
   Platform,
   EmitterSubscription,
@@ -347,7 +348,14 @@ function _onAppStateChangeAndroid(state: AppStateStatus) {
   }
 
   if (state === 'active' && _onWebBrowserCloseAndroid) {
-    _onWebBrowserCloseAndroid();
+    const pendingClose = _onWebBrowserCloseAndroid;
+    queueMicrotask(() => {
+      requestAnimationFrame(() => {
+        if (_onWebBrowserCloseAndroid === pendingClose) {
+          pendingClose();
+        }
+      });
+    });
   }
 }
 
@@ -381,6 +389,20 @@ async function _openBrowserAndWaitAndroidAsync(
   return result;
 }
 
+function _afterAuthRedirectCatchup(): Promise<void> {
+  return new Promise((resolve) => {
+    requestAnimationFrame(() => {
+      requestAnimationFrame(() => {
+        requestAnimationFrame(() => {
+          InteractionManager.runAfterInteractions(() => {
+            resolve();
+          });
+        });
+      });
+    });
+  });
+}
+
 async function _openAuthSessionPolyfillAsync(
   startUrl: string,
   returnUrl?: string | null,
@@ -398,10 +420,16 @@ async function _openAuthSessionPolyfillAsync(
 
   try {
     if (Platform.OS === 'android') {
-      return await Promise.race([
-        _openBrowserAndWaitAndroidAsync(startUrl, browserParams),
-        _waitForRedirectAsync(returnUrl),
-      ]);
+      const redirectPromise = _waitForRedirectAsync(returnUrl);
+      const dismissPromise = _openBrowserAndWaitAndroidAsync(startUrl, browserParams);
+      let result = await Promise.race([dismissPromise, redirectPromise]);
+      if (result.type === WebBrowserResultType.DISMISS || result.type === WebBrowserResultType.CANCEL) {
+        const late = await Promise.race([redirectPromise, _afterAuthRedirectCatchup()]);
+        if (late && late.type === 'success' && late.url) {
+          result = late;
+        }
+      }
+      return result;
     } else {
       return await Promise.race([
         openBrowserAsync(startUrl, browserParams),
@@ -430,11 +458,19 @@ function _stopWaitingForRedirect() {
   _redirectSubscription = null;
 }
 
+function _oauthRedirectUrlMatches(returnUrl: string, incoming: string): boolean {
+  if (incoming.startsWith(returnUrl)) {
+    return true;
+  }
+  const norm = (u: string) => u.replace(/^([a-z][a-z0-9+.-]*:)\/{2,}/i, (_, scheme) => `${scheme}/`);
+  return norm(incoming).startsWith(norm(returnUrl));
+}
+
 function _waitForRedirectAsync(returnUrl?: string | null): Promise<WebBrowserRedirectResult> {
   // Note that this Promise never resolves when `returnUrl` is nullish
   return new Promise((resolve) => {
     const redirectHandler = (event: RedirectEvent) => {
-      if (returnUrl && event.url.startsWith(returnUrl)) {
+      if (returnUrl && event.url && _oauthRedirectUrlMatches(returnUrl, event.url)) {
         resolve({ url: event.url, type: 'success' });
       }
     };
