diff --git a/node_modules/expo-keep-awake/src/index.ts b/node_modules/expo-keep-awake/src/index.ts
index b7a856e..4e76846 100644
--- a/node_modules/expo-keep-awake/src/index.ts
+++ b/node_modules/expo-keep-awake/src/index.ts
@@ -30,11 +30,14 @@ export function useKeepAwake(tag?: string, options?: KeepAwakeOptions): void {
 
   useEffect(() => {
     let isMounted = true;
-    activateKeepAwakeAsync(tagOrDefault).then(() => {
-      if (isMounted && ExpoKeepAwake.addListenerForTag && options?.listener) {
-        addListener(tagOrDefault, options.listener);
-      }
-    });
+    activateKeepAwakeAsync(tagOrDefault)
+      .then(() => {
+        if (isMounted && ExpoKeepAwake.addListenerForTag && options?.listener) {
+          addListener(tagOrDefault, options.listener);
+        }
+      })
+      // Android: Activity may not exist yet when Expo's dev withDevTools() mounts (CurrentActivityNotFoundException).
+      .catch(() => {});
     return () => {
       isMounted = false;
       if (options?.suppressDeactivateWarnings) {
diff --git a/node_modules/expo-keep-awake/tsconfig.json b/node_modules/expo-keep-awake/tsconfig.json
index ae1ae0d..a246fd2 100644
--- a/node_modules/expo-keep-awake/tsconfig.json
+++ b/node_modules/expo-keep-awake/tsconfig.json
@@ -1,8 +1,15 @@
 {
-  "extends": "expo-module-scripts/tsconfig.base",
   "compilerOptions": {
-    "outDir": "./build",
-    "emitDeclarationOnly": true
+    "target": "ESNext",
+    "module": "preserve",
+    "moduleResolution": "bundler",
+    "strict": true,
+    "skipLibCheck": true,
+    "noEmit": true,
+    "jsx": "react-native",
+    "allowJs": true,
+    "esModuleInterop": true,
+    "resolveJsonModule": true
   },
   "include": ["./src"],
   "exclude": ["**/__mocks__/*", "**/__tests__/*", "**/__rsc_tests__/*"]
